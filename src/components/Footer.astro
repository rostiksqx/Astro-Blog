<footer
  class="flex flex-col gap-2 sm:flex-row py-6 w-full shrink-0 items-center px-4 md:px-6 border-t bg-[#3b2a22] text-white"
>
  <p class="text-xs">© 2024 Coffee Shop. All rights reserved.</p>
  <nav class="sm:ml-auto flex gap-4 sm:gap-6">
    <a
      href="#shop"
      class="text-sm font-medium hover:underline underline-offset-4 scroll-link"
    >
      Shop
    </a>
    <a
      class="text-sm font-medium hover:underline underline-offset-4 scroll-link"
      href="#about"
    >
      About
    </a>
    <a
      class="text-sm font-medium hover:underline underline-offset-4 scroll-link"
      href="#contact"
    >
      Contact Us
    </a>
    <div class="flex gap-2">
      <a class="text-gray-400 hover:text-white" href="#">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          class="h-5 w-5"
        >
          <path
            d="M22 4s-.7 2.1-2 3.4c1.6 10-9.4 17.3-18 11.6 2.2.1 4.4-.6 6-2C3 15.5.5 9.6 3 5c2.2 2.6 5.6 4.1 9 4-.9-4.2 4-6.6 7-3.8 1.1 0 3-1.2 3-1.2z"
          ></path>
        </svg>
        <span class="sr-only">Twitter</span>
      </a>
    </div>
  </nav>
</footer>

<script>
  import { onMount } from "solid-js";

  onMount(() => {
    const scrollLinks = document.querySelectorAll(".scroll-link");

    scrollLinks.forEach((link) => {
      link.addEventListener("click", (e) => {
        e.preventDefault();

        const href = link.getAttribute("href");
        const targetElement = document.querySelector(href || "") as HTMLElement;

        if (targetElement) {
          const offsetTop = targetElement.offsetTop;

          window.scroll({
            top: offsetTop,
            behavior: "smooth",
          });
        }
      });
    });
  });
</script>
